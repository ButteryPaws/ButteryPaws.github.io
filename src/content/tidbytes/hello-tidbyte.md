---
title: 'First tidbyte'   # optional — omit for a title-less note
date: 2026-08-16
tags: ['meta']
draft: true
---

First tidbit. This section is for tweet-sized things: a small story, a joke, or
just a meme. Write Markdown here, including `inline code`, [links](/), and
images for memes:

```
git commit -m "fix"
git commit -m "actually fix"
git commit -m "please"
```

Drop an image in `public/` and reference it like `![alt](/my-meme.png)`.
